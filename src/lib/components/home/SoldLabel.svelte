<div><p>Vendido!</p></div>

<style lang="scss">
  div {
    background-color: #fffd;
    border-radius: var(--radius20);
    color: var(--danger600);
    border: 3px solid var(--danger600);
    font-weight: 600;
    font-family: var(--logoFont);
    font-size: var(--fz80);
    padding: var(--gap20) var(--gap40);
    position: absolute;
    top: -30px;
    right: 0px;
    left: unset;
    rotate: 15deg;
    box-shadow:
      0 0 0 3px #fffd,
      var(--bs30);
    @media only screen and (min-width: 1200px) {
      top: 20px;
      left: 260px;
      right: unset;
    }
    p {
      margin: 0;
      line-height: 1.2;
    }
  }
</style>
